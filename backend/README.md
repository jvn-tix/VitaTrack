# VitaTrack Express Backend

Contoh backend sederhana menggunakan Express untuk `register`, `login`, dan `me`.

## Instalasi

```bash
cd backend
npm install
```

## Jalankan

Buat file `.env` berdasarkan `.env.example` lalu jalankan:

```bash
npm start
```
```
